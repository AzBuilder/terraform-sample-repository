name: Build

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    runs-on: ubuntu-latest  # windows-latest | macos-latest
    name: Test changed-files
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # OR "2" -> To retrieve the preceding commit.
      
      # Example 1
      #- name: Get changed files
      #  id: changed-files
      #  uses: tj-actions/changed-files@v31
      #  with:
      #    dir_names: "true"

      #- name: List all changed files
      #  run: |
      #    echo "$SAMPLE"
      #    for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
      #      echo "$file was changed"
      #    done
      #  env:
      #    SAMPLE: ${{ steps.changed-files.outputs.all_changed_files }}
    
      #- name: Set output
      #  id: vars
      #  run: echo ::set-output name=short_ref::${GITHUB_REF#refs/*/}

      #- name: Check output
      #  run: echo ${{ github.head_ref }}

      - uses: AzBuilder/terrakube-action-github@main
        with:
          TERRAKUBE_TOKEN:  ${{ secrets.TERRAKUBE_PAT }} 
          TERRAKUBE_TEMPLATE: "Terraform-Plan"
          TERRAKUBE_REPOSITORY: ${{ format('{0}/{1}.git', secrets.FOLDER, secrets.FILENAME) }}
          TERRAKUBE_ENDPOINT: ${{ secrets.TERRAKUBE_GITPOD }}  
          TERRAKUBE_BRANCH: ${{ github.head_ref }}
          TERRAKUBE_ORGANIZATION: "simple"
          TERRAKUBE_SHOW_OUTPUT: true
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
